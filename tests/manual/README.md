# Manual Validation

This directory is for live or operator-driven validation that should not run as part of the default pytest suite.

Typical reasons:

- requires real API credentials
- depends on local model assets
- is intended for exploratory or demo-style validation rather than CI
