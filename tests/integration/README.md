# Integration Coverage

This directory groups tests that exercise broader runtime surfaces such as:

- browser-backed flows
- Discord delivery
- STT or VLM integrations
- end-to-end interaction paths

These tests remain public because they show real coverage, but they are separated from the core regression layer so the repository is easier to scan.
